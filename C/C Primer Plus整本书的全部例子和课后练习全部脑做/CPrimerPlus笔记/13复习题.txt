1.文件中有定义，需要头文件#include<stdio.h>,非文件指针存储fopen()返回值，
	未指定文件打开模式，fclose需要文件指针非文件名
	fputs()参数顺序反了，应在字符串结尾添加换行符，fputs不自动添加
2.通过命令行第一个参数指定的文件，并且输出文件中的所有数字字符
3.	a:ch = getc(fp1);
	b:fprintf(stdout,"%c",ch);
	c:putc(ch,fp2);
	d:fclose(fp1);
4.make
5.make
6.	二进制文件和文本文件对文件系统的依赖性不同
	二进制流和文本流之间的不同在于读写文件时，二进制不进行转换，文本流进行转换(MS_DOS:\r\n转换为C识别的\n,Macintosh:\r转换为\n)
7.	fprintf()将8238201作为在7个字节中的7个字符来保存;fwrite()将8238201的二进制表示存储为4字节的整数
	putc()和frwrite()都将字符S保存为单字节的二进制码
8.	1、2相同，第一个为速记;写到到标准错误上，通常标准错误重定向到和标准输出相同的位置，但是标准错误不受标准输出的重定向影响
9.r+更新读模式可以再文件中的任何位置读写，所以更合适;a+只能在已有文件内容之后进行写入，w+会将文件清空

